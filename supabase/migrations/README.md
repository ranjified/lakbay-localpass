# Supabase Migrations

Place timestamped migration files here as the project moves from demo scripts to production database changes.

The current demo still uses:

- `supabase/schema.sql`
- `supabase/seed.sql`
- `supabase/policies.sql`

When Supabase CLI migrations are introduced, copy the schema and policy changes into ordered migration files instead of editing only the root SQL scripts.
